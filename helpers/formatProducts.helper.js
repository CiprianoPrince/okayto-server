const formatProducts = (foundProducts) => {
    const formattedProducts = foundProducts.map((foundProduct) => {
        return {};
    });
    return formattedProducts;
};

module.exports = formatProducts;
